# Why dynamic typed input exist?

For example, in conditions, someone picks a variable of type date x and wants to compare it with a second value. This second value may be another variable of that type or a date selected from the date picker. Normally, we would show a text input, but we don’t want people to enter dates manually when we have a fully functional date picker in the application.
