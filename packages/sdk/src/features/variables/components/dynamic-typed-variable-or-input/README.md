# Why dynamic typed variable or input exist?

Sometimes we want the user to pick a variable from a predefined list or set the value manually (but if the type is known, it should be a date picker, for example).

This component gives us a toggle between those two cases.
