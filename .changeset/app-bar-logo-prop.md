---
'@workflowbuilder/sdk': minor
---

`<WorkflowBuilder.Root>` accepts `logo` (an image URL, `{ light, dark }` per-theme URLs, or a custom element - replaces the built-in app-bar logo) and `logoHref` (wraps it in a link).
