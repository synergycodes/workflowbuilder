---
'@workflowbuilder/sdk': patch
---

The palette's "Templates" action renders as `ghost-secondary`, the variant the design library binds for it. It kept `secondary` through the button redesign, and that name changed meaning: it used to be the outlined treatment and is now the solid grey one, so the button had turned into a filled slab.
