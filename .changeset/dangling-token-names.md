---
'@workflowbuilder/sdk': patch
---

Fix CSS variable names that pointed at non-existent tokens: neutral and warning form-message variants render their intended colors again, and the variables-settings modal picks up its token-defined colors, spacing, and radii.
