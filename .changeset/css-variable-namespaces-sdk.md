---
'@workflowbuilder/sdk': major
---

Public SDK controls move to `--wb-public-*`: rename `--wb-background-color` to `--wb-public-background-color`, `--wb-transition` to `--wb-public-transition`, and each `--wb-scroll-*` variable to `--wb-public-scroll-*`. Private SDK variables move from `--wb-<name>` to `--wb-sdk-<name>` with no compatibility aliases.
