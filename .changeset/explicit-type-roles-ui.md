---
'@workflowbuilder/ui': minor
---

Make typography roles carry their font families and add the public `--wb-public-font-family` and `--wb-public-font-family-mono` overrides. Legacy `ax-public-*` typography classes also use the public family lever until they are removed.
