---
'@workflowbuilder/sdk': minor
---

`JsonForms` is exported beside the other JsonForms primitives, so a custom control can render a separate schema and data with the editor's own renderers and validator.
