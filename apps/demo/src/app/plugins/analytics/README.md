# Analytics

Optional plugin slot for wiring analytics or telemetry into the **Workflow Builder** app.

Use this folder to register listeners for diagram events (node added, edge connected, template loaded, etc.) and forward them to your analytics backend of choice. This entry exists as a stub so consumers can drop in their own implementation without editing `app.tsx`.
